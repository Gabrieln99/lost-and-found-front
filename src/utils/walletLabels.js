// Shown on a button while a wallet signature prompt is open. Shared by
// useListingActions (labelFor) and ListingMessageThread (sendButtonLabel)
// so the two copies can't drift.
export const WALLET_SIGNING_LABEL = 'Waiting for you to confirm in your wallet...'
